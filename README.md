# cctools-build.linux
A script for building iOS apps.

### Dependencies
#### pre dependencies
[`apple/llvm-clang >= 10.0`](https://github.com/apple/llvm-project)

#### build dependencies
`subversion >= 1.0`
